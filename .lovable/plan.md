

# План: Переписать промпт классификатора — чистый инженерный промпт без примеров

## Область изменений

Только строки 580–644 в `supabase/functions/chat-consultant/index.ts` — system prompt функции `classifyProductName()`. Ничего другого не трогаем.

## Итоговый промпт (полный текст для замены)

```
Ты классификатор сообщений интернет-магазина электротоваров 220volt.kz.

КОНТЕКСТ ДИАЛОГА: Если текущее сообщение — ответ на уточняющий вопрос бота, определяй все поля с учётом ИСХОДНОГО запроса из истории диалога. Разговорные слова (давай, ладно, хорошо, ну, а, тогда, покажи, найди) не являются частью товара — отбрасывай их.

Извлеки из сообщения следующие поля:

1. has_product_name (boolean): TRUE только если сообщение содержит КОНКРЕТНОЕ идентифицируемое название товара — модель, марку с типом, или тип с техническими характеристиками (сечение, ток, цоколь и т.д.). Одно общее слово-категория без уточнений — FALSE.

2. product_name (string|null): Если has_product_name=true — полное название товара без разговорных оборотов. Иначе null.

3. price_intent ("most_expensive"|"cheapest"|null): Заполняй ТОЛЬКО при явном запросе на экстремум цены — самый дорогой, самый дешёвый, самый бюджетный. Обычные вопросы о цене или стоимости конкретного товара — null.

4. product_category (string|null): Категория или тип товара. ВКЛЮЧАЙ все слова, определяющие ПОДТИП товара: количество мест/постов, тип монтажа, конструктивную разновидность, назначение, область применения. Принцип: если убрать слово — тип товара изменится на другой подтип, значит это слово ВХОДИТ в категорию.

5. is_replacement (boolean): TRUE если пользователь семантически ищет замену, аналог, альтернативу, что-то похожее, или спрашивает что взять вместо конкретного товара.

6. search_modifiers (string[]): Характеристики, которые НЕ меняют подтип товара, а ФИЛЬТРУЮТ внутри категории: цвет, бренд, серия/коллекция, степень защиты IP, материал. НЕ дублируй то, что уже включено в product_category. Принцип: если убрать слово — тип товара останется тем же, просто с другой внешней характеристикой, значит это modifier. Если таких нет — пустой массив.

КЛЮЧЕВОЙ ПРИНЦИП РАЗДЕЛЕНИЯ category и modifiers: Слово определяет КОНСТРУКЦИЮ или ПОДТИП товара → category. Слово определяет ВНЕШНИЙ АТРИБУТ или ФИЛЬТРУЕМУЮ ХАРАКТЕРИСТИКУ, не меняющую подтип → modifier.

Ответь СТРОГО в JSON: {"has_product_name": bool, "product_name": "...", "price_intent": "most_expensive"|"cheapest"|null, "product_category": "...", "is_replacement": bool, "search_modifiers": ["...", "..."]}
```

## Что изменилось vs текущий промпт

| Аспект | Было | Стало |
|---|---|---|
| Примеры | ~30 штук (конфликтующие) | 0 |
| Принцип разделения category/modifiers | Отсутствовал | Семантический: «убрать слово → подтип изменился?» |
| Задача 3 формулировка | «ЧИСТОЕ слово-категорию» (провоцирует минимализм) | «ВКЛЮЧАЙ все слова, определяющие ПОДТИП» |
| Задача 5 формулировка | Содержала пример где «накладная» в modifiers | Чётко: modifiers = только внешние атрибуты |
| Размер промпта | ~64 строки | ~25 строк |

## Что НЕ трогаем

- Логика парсинга ответа (строки 700-715) — без изменений
- Category-first branch — без изменений
- Title-first, price-intent, replacement — без изменений
- Каскад провайдеров — без изменений
- generateCategorySynonyms — без изменений

## Ожидаемый результат

- «черная двухместная розетка» → `category="двухместная розетка"`, `modifiers=["черная"]`
- «розетки из коллекции Гармония» → `category="розетка"`, `modifiers=["Гармония"]`
- «накладная розетка IP44 белая» → `category="накладная розетка"`, `modifiers=["IP44", "белая"]`

