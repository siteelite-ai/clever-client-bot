

# План: Заменить discoverOptionKeys на микро-LLM резолв фильтров

## Суть

Вместо ручного fuzzy-matching в `discoverOptionKeys` (строки 1990-2100) — один вызов микро-LLM, которая видит реальную схему характеристик найденных товаров и сама определяет правильный маппинг.

---

## Промпт для resolveFiltersWithLLM (от промпт-инженера)

```text
Ты резолвер фильтров товаров интернет-магазина электротоваров.

ЗАДАЧА: Для каждого модификатора из запроса пользователя определи, какой характеристике товара он соответствует, и подбери точное значение из доступных.

СХЕМА ХАРАКТЕРИСТИК КАТЕГОРИИ:
{schema}

МОДИФИКАТОРЫ ПОЛЬЗОВАТЕЛЯ:
{modifiers}

ПРАВИЛА РЕЗОЛВА:
1. Каждый модификатор соотноси с характеристикой по СМЫСЛУ, а не по совпадению символов. Учитывай единицы измерения, физический смысл и контекст категории товара.
2. Значение выбирай СТРОГО из списка доступных значений характеристики. Возвращай значение В ТОЧНОСТИ как оно указано в схеме — без изменения регистра, пробелов или формата.
3. Если модификатор не соответствует ни одной характеристике или подходящего значения нет в списке — не включай его в результат.
4. Если модификатор может подойти к нескольким характеристикам, выбирай ту, которая семантически ближе к запросу пользователя в контексте данной категории товаров.

Ответь СТРОГО в JSON: {"filters": {"api_key": "exact_value", ...}}
Если ни один модификатор не удалось сопоставить — верни {"filters": {}}
```

Где `{schema}` формируется из продуктов Pass 1:
```text
api_key_1 (Название характеристики): значение1, значение2, значение3
api_key_2 (Название характеристики): значение1, значение2
...
```

Где `{modifiers}` — массив строк из классификатора: `["50 м", "IP44"]`

### Оценка промпт-инженера

Промпт спроектирован по следующим принципам:

- **Описательный, без примеров** — полностью соответствует инженерному подходу проекта
- **Контекстно-зависимый** — LLM видит реальные характеристики конкретной категории, а не угадывает глобально
- **Строгий формат вывода** — JSON с конкретной структурой, temperature=0
- **Защита от ложных срабатываний** — правило 3 явно запрещает включать несопоставленные модификаторы
- **Защита от «частота вместо длины»** — правило 1 требует учитывать единицы измерения и физический смысл

---

## Технические изменения (1 файл: `chat-consultant/index.ts`)

### 1. Новая функция `resolveFiltersWithLLM` (~50 строк)

- Принимает: `products: Product[]`, `modifiers: string[]`, `settings: AppSettings`
- Собирает `optionSchema` из products (агрегация опций по apiKey → caption + уникальные values)
- Формирует промпт с подстановкой schema и modifiers
- Вызывает микро-LLM через тот же каскад провайдеров что и классификатор (OpenRouter → Google → Gateway)
- Парсит JSON-ответ → возвращает `Record<string, string>`
- При ошибке возвращает `{}`

### 2. Изменить вызов в `searchProductsMulti` (строки 2259-2262)

Заменить:
```typescript
const resolvedFilters = discoverOptionKeys(Array.from(productMap.values()), humanFilters);
```

На вызов `resolveFiltersWithLLM` с передачей products, modifiers и settings.

Для этого нужно прокинуть `settings` и `modifiers` в `searchProductsMulti` — добавить параметры.

### 3. Прокинуть settings и modifiers в searchProductsMulti

В вызове из category-first ветки (строка 3007) передать `appSettings` и `modifiers` как дополнительные параметры.

### 4. Старый discoverOptionKeys — НЕ удаляем, НЕ используем как fallback

По указанию пользователя — fallback не ставим. Функция остаётся в коде но не вызывается из нового пути.

---

## Что НЕ трогаем

- Промпт классификатора (строки 580-600) — работает
- Приоритизацию запросов с модификаторами (строки 2978-2993) — работает
- Cap=6 — работает
- Title-first, price-intent, replacement ветки — не затронуты
- generateCategorySynonyms — не затронута

## Объём: ~60 строк нового кода, ~5 строк правок в существующем

